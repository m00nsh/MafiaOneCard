# MafiaRoom.ts 로직 문서

## 1. 방 초기화 및 상태 (`onCreate`)
- **상태(State)**: `GameStateSchema`를 초기화합니다.
    - `status`: "LOBBY" (로비)
    - `direction`: "clockwise" (시계 방향)
    - `attackStack`: 0 (공격 스택)
    - `deckCount`: 0 (덱 매수)
    - `topCard`: 빈 카드로 초기화 (게임 시작 전)
- **데이터 구조**: `deck`(덱)과 `discardPile`(버린 카드 더미)은 보안을 위해 클라이언트와 동기화하지 않는 private 배열로 관리합니다.

## 2. 플레이어 관리
- **입장 (`onJoin`)**:
    - `PlayerSchema`를 생성합니다.
    - 방에 처음 들어온 플레이어가 **방장(Host)**이 됩니다.
    - 닉네임이 제공되면 설정합니다.
- **퇴장 (`onLeave`)**:
    - 게임 상태가 `PLAYING`(진행 중)일 경우: `handlePlayerElimination`을 호출하여 탈락 처리합니다.
    - `LOBBY`(로비)일 경우: 상태에서 플레이어를 즉시 제거합니다.
- **탈락 처리 (`handlePlayerElimination`)**:
    - **발동 조건**: 플레이어가 게임 도중 나가거나, '파산(Burst)'(핸드 20장 초과)된 경우.
    - **로직**:
        1.  **턴 넘김**: 탈락한 플레이어의 차례였다면, 플레이어를 제거하기 **전에** 다음 사람에게 턴을 넘깁니다.
        2.  **랭킹 산정**: 현재 남은 인원수를 기준으로 등수를 매깁니다. (예: 3명 남았을 때 나가면 3등)
        3.  **카드 반환**: 탈락한 플레이어의 손패를 모두 `deck`으로 회수하고 덱을 다시 섞습니다.
        4.  **알림**: 탈락한 플레이어에게 개별적으로 `game_end` 메시지(결과 통계 포함)를 보내고, 다른 플레이어들에게는 안내 방송을 보냅니다.
        5.  **최후의 1인**: 1명만 남게 되면 그 사람을 승자로 선언하고 게임을 종료합니다.

## 3. 게임 시작 흐름
- **준비 체크 (`onMessage("ready")`)**: 플레이어의 준비 상태(`isReady`)를 토글합니다.
- **시작 조건 확인 (`checkStartGame`)**:
    - 최소 2명 이상의 플레이어.
    - 모든 플레이어가 `isReady: true` 상태여야 함.
- **게임 준비 (`prepareGame`)**:
    1.  **덱 생성**: 54장 (일반 52장 + 흑백 조커 + 컬러 조커).
    2.  **셔플**: 피셔-예이츠 알고리즘으로 카드를 섞습니다.
    3.  **분배**: 각 플레이어에게 카드 7장씩 나누어 줍니다.
    4.  **초기 카드**: 덱에서 한 장을 뽑아 `discardPile`에 놓고 `topCard`로 설정합니다.
    5.  **첫 턴**: 플레이어 목록의 첫 번째 사람을 `currentTurn`으로 설정합니다.

## 4. 카드 플레이 로직 (`onMessage("card_play")`)
### A. 검증 (Validation)
1.  **턴 확인**: 현재 요청한 클라이언트의 차례인지 확인합니다.
2.  **공격 방어**:
    - `attackStack > 0`인 경우: 플레이어는 반드시 **공격 카드**를 내야 합니다.
    - **공격 위계 규칙**: 낸 카드의 공격력이 현재 `topCard`의 공격력보다 **크거나 같아야** 합니다.
    - 위계: `2 (2)` < `A (3)` < `흑백 조커 (5)` < `컬러 조커 (8)`.
3.  **낼 수 있는 카드인지 확인**:
    - `topCard`와 **문양(Suit)**이 같거나 **숫자(Rank)**가 같아야 합니다.
    - **문양 변경(7 카드) 예외**: `selectedSuit` 상태가 활성화되어 있다면, 반드시 **변경된 문양**을 내야 합니다. (혹은 같은 숫자 7이나 조커)
    - 조커는 언제든 낼 수 있습니다.

### B. 실행 (Execution)
- **카드 이동**: 손패에서 제거 -> `topCard` 업데이트 -> 기존 `topCard`를 `discardPile`로 이동.
- **문양 변경 ('7' 카드)**:
    - '7' 카드를 내면서 문양을 선택한 경우: `state.selectedSuit`를 업데이트합니다.
    - '7'이 아닌 카드를 낸 경우: `state.selectedSuit`를 초기화합니다.
- **특수 효과**:
    - **공격 카드 (A, 2, 조커)**: `attackStack`에 해당 공격력을 누적시킵니다.
    - **J (Jump)**: 다음 차례를 건너뜁니다 (`shouldSkipTurn = true`).
    - **Q (Reverse)**: 진행 방향을 반전시킵니다 (`direction` 변경).
    - **K (King)**: 한 장 더 낼 수 있습니다 (턴 유지, `shouldKeepTurn = true`).
    - **일반 카드**: (공격 스택이 비어있을 때) `attackStack`을 0으로 확실히 초기화합니다.
- **승리 조건**: 손패가 0장이 되면:
    //원카드 외치기 추가할건지//
    - 게임 상태 -> `ENDED`.
    - 남은 카드 수(1순위)와 턴 거리(2순위)로 순위를 산정합니다.
    - `game_end` 메시지를 방송합니다.

### C. 턴 전환
- `nextTurn(skipNext)`를 호출하여 턴을 넘깁니다.
- 'K' 카드를 낸 경우(`shouldKeepTurn`)에는 호출하지 않고 턴을 유지합니다.

## 5. 드로우 로직 (`onMessage("draw_card")`)
- **검증**: 플레이어의 차례인지 확인합니다.
- **드로우 개수**: `attackStack`이 있으면 그만큼, 없으면 1장을 뽑습니다.
- **진행 과정**:
    1.  `deck`에서 카드를 뽑습니다.
    2.  **보충**: 뽑는 도중 `deck`이 비면 `replenishDeck()`을 호출해 `discardPile`을 `deck`으로 섞어 넣고 계속 뽑습니다.
    3.  **빈 덱 방지**: 카드를 다 뽑은 직후 덱이 정확히 0장이 되었다면(`deck.length === 0`), 즉시 보충하여 `deckCount: 0` 상태가 유지되는 것을 방지합니다.
- **공격 초기화**: 공격에 대한 방어책으로(또는 페널티로) 카드를 뽑았으므로, `attackStack`을 0으로 초기화합니다.
- **턴 종료**: 카드를 뽑으면 자동으로 다음 사람에게 턴이 넘어갑니다.
- **파산 확인**: 뽑은 후 손패가 20장을 넘으면 `handlePlayerElimination(..., 'burst')`를 호출합니다.

## 6. 헬퍼 메서드
- **`replenishDeck()`**: `discardPile`에 있는 카드들을 `deck`으로 옮기고, `discardPile`을 비운 뒤 덱을 섞습니다.
- **`getAttackValue(card)`**: 카드의 공격력 수치를 반환합니다 (컬러 조커=8, 흑백 조커=5, A=3, 2=2, 그 외=0).
- **`getTurnDistance(...)`**: 현재 턴과 대상 플레이어 사이의 턴 거리를 계산합니다 (랭킹 산정 시 동점자 처리에 사용).
